import {{pascalCase name}} from './{{pascalCase name}}';

import { connect } from 'react-redux'

const makeMapStateToProps = state => {

}

const makeDispatchToPros = () => ({

})

export default connect(
  makeMapStateToProps,
  makeDispatchToPros
)({{pascalCase name}})